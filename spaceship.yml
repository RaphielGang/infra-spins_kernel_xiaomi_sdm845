kind: pipeline
name: Disrupt Kernelcore Droneship

steps:
  - name: docker
    image: docker:latest
    environment:
      OWO:
        from_secret: YEET
    volumes:
      - name: dockersock
        path: /var/run/docker.sock
    command: 
      - ./build.sh

volumes:
- name: dockersock
  host:
    path: /var/run/docker.sock